name = "switchbot-remote"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "./public"
binding = "ASSETS"
# HTML だけ Worker を先に通す（バージョンを埋め込むため）。他のアセットは直接配信のまま
run_worker_first = ["/", "/index.html"]
